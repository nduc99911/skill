# 2026-04-25

- Built and operationalized `skills/affiliate-book-marketing-director` with live-tested comment triage improvements:
  - Removed private reply flow due to repeated Meta permission/API failures (`/private_replies`, code 100 subcode 33).
  - Enforced public threaded replies via comment endpoint (`/{comment_id}/comments`).
  - Added intent routing: `buy_intent` (link reply), `casual` (context reply), `negative_or_spam` (skip).
  - Added affiliate-link safety: if link is blank/`NONE`, downgrade buy-intent to casual (no broken-link posting).

- Dispatch robustness and observability improved:
  - Added graceful fallback publish mode when Cloudflare creds are missing.
  - Added Telegram notifier for dispatch success/error.
  - Added deeper Meta API error parsing and delete helper in `meta_api.py`.

- Shipped OpenClaw Meta Ads MVP architecture under `openclaw-meta-ads/`:
  - `meta-ads-api-core`, `meta-ads-insights-reporter`, `meta-ads-optimizer`, `meta-ads-copywriter`, `meta-ads-creative-matrix`, `meta-ads-campaign-drafter`, and `meta-ads-orchestrator`.
  - Confirmed live insights pull for ad account `act_126735987741182`.

- Added pre-flight safety in `meta-ads-campaign-drafter`:
  - `validate_post_ready(payloads_list)` checks media asset presence (`image_hash`/`video_id`), destination URL + UTM warning, adset budget floor, and `PAUSED` status across all levels.
  - Blocks API writes on missing required fields with explicit `BLOCKED` result.

- Professional packaging completed:
  - Created `openclaw-meta-ads/README.md` (MVP overview, 6 skills, env setup, orchestrator workflow, guardrails).
  - Initialized git repo in `openclaw-meta-ads/`, committed initial architecture, and pushed to `https://github.com/nduc99911/ads-fb`.

- Critical incident and remediation:
  - User reported forced dispatch posted two next-day items early (schedule violation).
  - Immediate cleanup executed: deleted wrong posts via Meta API; kept correct post (`Đắc Nhân Tâm`).
  - Queue restored: affected items reset to `queued` with intended 26/04 publish times.
  - Root-cause fix implemented: dispatch time parsing now honors both `publish_date` + `publish_time`; hard gating ensures only `publish_at <= now` can dispatch.
