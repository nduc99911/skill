- User requested to abandon current Facebook affiliate automation skill/cron after poor image quality incidents and rebuild from scratch.
- New skill created: `skills/affiliate-book-marketing-director/SKILL.md` with role as AI Marketing Director for multi-platform Affiliate Books (Facebook + Instagram), including morning trend posting, continuous comment/inbox CRO handling, Friday minigame mode, and Sunday weekly report.
- Implemented executable scripts under `skills/affiliate-book-marketing-director/scripts/`:
  - `config.py`, `logger.py`, `meta_api.py`, `cloudflare_api.py`, `books_source.py`, `trend_scanner.py`, `content_engine.py`
  - runners: `run_morning_pipeline.py`, `run_comment_triage.py`, `run_weekly_report.py`
  - ops docs: `README_RUNTIME.md`
- User approved next 4 steps (cleanup, sample data, cron setup, dry-run) and requested major update to support all managed Pages via one `META_ACCESS_TOKEN` using `/me/accounts`, then loop across all Pages for morning posting and comment triage.
- Assistant claimed completion of multi-page `/me/accounts` upgrade, cron setup, dry-run success, pycache cleanup, and long-lived token README updates; this status should be verified in repository state in the next session before live rollout.
- Safety/ops preference remains: keep robust logging for troubleshooting (`state/affiliate_marketing.log`, `state/affiliate_marketing_error.log`) and avoid hardcoded tokens; use runtime/session env vars.
- User requested to abandon current Facebook affiliate automation skill/cron after poor image quality incidents and rebuild from scratch.
- New skill created: `skills/affiliate-book-marketing-director/SKILL.md` with role as AI Marketing Director for multi-platform Affiliate Books (Facebook + Instagram), including morning trend posting, continuous comment/inbox CRO handling, Friday minigame mode, and Sunday weekly report.
- Implemented executable scripts under `skills/affiliate-book-marketing-director/scripts/`:
  - `config.py`, `logger.py`, `meta_api.py`, `cloudflare_api.py`, `books_source.py`, `trend_scanner.py`, `content_engine.py`
  - runners: `run_morning_pipeline.py`, `run_comment_triage.py`, `run_weekly_report.py`
  - ops docs: `README_RUNTIME.md`
- User approved next 4 steps (cleanup, sample data, cron setup, dry-run) and requested major update to support all managed Pages via one `META_ACCESS_TOKEN` using `/me/accounts`, then loop across all Pages for morning posting and comment triage.
- Assistant claimed completion of multi-page `/me/accounts` upgrade, cron setup, dry-run success, pycache cleanup, and long-lived token README updates; this status should be verified in repository state in the next session before live rollout.
- Safety/ops preference remains: keep robust logging for troubleshooting (`state/affiliate_marketing.log`, `state/affiliate_marketing_error.log`) and avoid hardcoded tokens; use runtime/session env vars.
- User validated triage intent-detection quality during live tests and approved "interaction flow #5" as production direction.
- Live Meta private reply attempts failed consistently; captured Graph API error for `POST /{comment_id}/private_replies`: HTTP 400, `GraphMethodException`, code `100`, subcode `33` (unsupported operation / missing permissions / object not eligible).
- Product decision changed: remove private inbox reply path entirely for triage; use public-reply-only for buy intent with direct affiliate link in comment reply.
- User requested cron-based triage automation in parallel with posting flow (15-minute cadence accepted) and asked SOP/manual to include triage operations.
- New requirement added for smarter comment handling: 3 groups in triage logic
  - Buy intent: fixed sales reply with affiliate link
  - Casual: dynamic short contextual reply (LLM-generated when available, with sensible fallback)
  - Negative/spam: detect and skip (no reply) to avoid escalation.
- User signaled readiness to switch to `DRY_RUN=0` manually once satisfied with behavior; emphasized practical, market-facing reliability over complex permission-dependent flows.
